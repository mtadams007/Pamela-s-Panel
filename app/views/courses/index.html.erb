<div class="header-container">
<h1 class="text-center">Courses</h1></div>
<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Hours</th>
      </tr>
    </thead>
    <tbody>
      <% @courses.each do |course| %>
      <tr>
        <td> <a href="/courses/<%=course.id%>/cohorts"><%= course.name %></a></td>
        <td><%= course.hours %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
