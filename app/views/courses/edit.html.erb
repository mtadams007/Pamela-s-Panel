<%= bootstrap_form_for @course do |f| %>

    <%= f.text_field :name, value: @course.name%>

    <%= f.number_field :hours, value: @course.hours%>
    <%= f.submit %>
<% end %>

<%= link_to 'Drop', course_path(@course), method: :delete %>
